<Window x:Class="WinContextMenuManager.QuickAccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinContextMenuManager"
        Width="819" Height="542"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="TileButton" TargetType="Button">
            <Setter Property="Width" Value="108"/>
            <Setter Property="Height" Value="90"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF6FC"/>
                                <Setter Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CCE4F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EmptyTileButton" TargetType="Button" BasedOn="{StaticResource TileButton}">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#EBEBEB"/>
            <Setter Property="Opacity" Value="0.4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="White" Padding="12,8"
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <!-- Actions principales (gauche) -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="📋 Prédéfinis" Style="{StaticResource SecondaryButton}"
                            Padding="10,6" Margin="0,0,8,0" Click="Presets_Click"/>
                    <Button Content="⚙ Paramètres" Style="{StaticResource SecondaryButton}"
                            Padding="10,6" Click="Settings_Click"/>
                </StackPanel>

                <!-- Actions raccourcis (droite) -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="↻ Actualiser" Style="{StaticResource SecondaryButton}"
                            Padding="10,6" Margin="0,0,8,0" Click="Refresh_Click"/>
                    <Button Content="✎ Modifier la configuration" Style="{StaticResource SecondaryButton}"
                            Padding="10,6" Click="EditConfig_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Grille de raccourcis 4 × 6 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="ShortcutsGrid"
                  HorizontalAlignment="Center" VerticalAlignment="Center"
                  Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
