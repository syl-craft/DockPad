<Window x:Class="WinContextMenuManager.ShortcutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Raccourci rapide"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F3F3"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,14">
            <TextBlock x:Name="TxtHeader" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
        </Border>

        <!-- Contenu -->
        <StackPanel Grid.Row="1" Margin="24,20">

            <!-- Nom -->
            <Label Content="Nom *"/>
            <TextBox x:Name="TxtName" Margin="0,0,0,16"/>

            <!-- Type -->
            <Label Content="Type *"/>
            <ComboBox x:Name="CmbType" Margin="0,0,0,16"
                      SelectionChanged="CmbType_SelectionChanged">
                <ComboBoxItem Content="Lancer une commande"       Tag="RunCommand"/>
                <ComboBoxItem Content="Ouvrir un dossier"         Tag="OpenFolder"/>
                <ComboBoxItem Content="Ouvrir dans le navigateur" Tag="OpenUrl"/>
                <ComboBoxItem Content="Ouvrir un terminal"        Tag="OpenTerminal"/>
            </ComboBox>

            <!-- Commande / Chemin / URL -->
            <Label x:Name="LblCommand" Content="Commande *"/>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtCommand" Grid.Column="0"/>
                <Button x:Name="BtnBrowse" Grid.Column="1" Content="..." Width="36" Margin="6,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Click="BrowseCommand_Click"/>
            </Grid>

            <!-- Icône -->
            <Label Content="Icône (optionnel)"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtIconPath" Grid.Column="0"/>
                <Button Grid.Column="1" Content="..." Width="36" Margin="6,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Click="BrowseIcon_Click"/>
                <Border Grid.Column="2" Width="36" Height="36" Margin="6,0,0,0"
                        BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4">
                    <Image x:Name="ImgIconPreview" Stretch="Uniform" Margin="4"/>
                </Border>
            </Grid>

        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" Padding="24,12" Margin="0,16,0,0"
                BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Style="{StaticResource SecondaryButton}"
                        Margin="0,0,12,0" Click="Cancel_Click"/>
                <Button Content="Enregistrer" Style="{StaticResource PrimaryButton}"
                        Click="Save_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
